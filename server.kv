MainInterface:
<MainInterface>:
    orientation: 'vertical'
    padding: dp(50)
    spacing: dp(30)
    Label:
        text:'Server is running.....\n'
        halign:'center'
    TextInput:
        readonly:True
        text:f'Copy this Address {root.display} to your game'
        id: display